#!/usr/bin/env ruby

require 'require_all'
require 'logging'
require_all 'lib'
require_all 'modules'

Yarpler::CLI.start(ARGV, debug: true)

module Yarpler
  def self.hi(n = 'Default Name')
    hail = Hail
    Hail.name(n)
  end
end
