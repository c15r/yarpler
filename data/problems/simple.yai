domain {
      class Dienst {
        var integer anzahl;
      }

      class Mitarbeiter {
        reference(Dienst) tage;
      }
}

initial {
  d1 = new Dienst(anzahl = 1..1);
  d2 = new Dienst(anzahl = 1..1);
  d3 = new Dienst(anzahl = 1..1);
  f = new Dienst(anzahl = 0..1000);

}