domain {
      class Mitarbeiter {
        const Tag tage;
      }

      class Dienst {
        const integer anzahl;
      }

      class Tag {
    	const integer index;
        var Dienst dienst;
      }
}

initial {
    d1 = new Dienst(anzahl=3);
    d2 = new Dienst(anzahl=7);
    f = new Dienst(anzahl=0);
}